<style>
  .wide {
    width: 98%;
  }
</style>

<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, 'Tittel' %><br />
    <%= f.text_field :title, :class => 'wide' %>
  </div>
  <div class="field">
    <%= f.label :email, 'Epost' %><br />
    <%= f.text_field :email, :class => 'wide' %>
  </div>
  <div class="field">
    <%= f.label :leadin, 'Ingress' %><br />
    <%= f.text_area :leadin, :rows => 2, :class => 'wide' %>
  </div>
  <div class="field">
    <%= f.label :body, 'Brødtekst' %><br />
    <%= f.text_area :body, :rows => 10, :class => 'wide' %>
  </div>
  <div class="actions">
    <%= f.submit 'Lagre' %>
  </div>
<% end %>
